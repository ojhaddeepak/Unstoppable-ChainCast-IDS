[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "frontend/.next"

[build.environment]
  NEXT_PUBLIC_API_URL = "/api"
  NODE_VERSION = "20.0.0"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
